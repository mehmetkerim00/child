# core_l10n

Строки UI на русском и туркменском (`lib/l10n/app_ru.arb`, `app_tk.arb`).
Хардкод строк в приложениях запрещён. Для `tk` системные строки Material
берутся из русского — во Flutter туркменского нет.
