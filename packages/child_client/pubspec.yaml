name: child_client
description: Клиент API, сгенерированный Serverpod (не править руками — `melos run generate:server`).
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  serverpod_client: 3.4.13
