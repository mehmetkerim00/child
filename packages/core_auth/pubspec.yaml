name: core_auth
description: Общий экран входа по телефону и SMS-коду для приложений child.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_data:
    path: ../core_data
  core_l10n:
    path: ../core_l10n
  core_ui:
    path: ../core_ui
  flutter_riverpod: ^2.6.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
