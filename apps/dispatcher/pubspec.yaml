name: dispatcher
description: "Приложение child: dispatcher."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_data:
    path: ../../packages/core_data
  core_domain:
    path: ../../packages/core_domain
  core_l10n:
    path: ../../packages/core_l10n
  core_ui:
    path: ../../packages/core_ui
  flutter_riverpod: ^2.6.1
  go_router: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
