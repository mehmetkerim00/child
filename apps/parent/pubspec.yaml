name: parent
description: "Приложение child: parent."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  core_auth:
    path: ../../packages/core_auth
  core_data:
    path: ../../packages/core_data
  core_domain:
    path: ../../packages/core_domain
  core_l10n:
    path: ../../packages/core_l10n
  core_ui:
    path: ../../packages/core_ui
  flutter_riverpod: ^2.6.1
  # Карта поездки: OSM-тайлы с обязательным кэшем (usage policy).
  cached_network_image: ^3.4.1
  flutter_map: ^8.3.1
  go_router: ^17.0.0
  latlong2: ^0.9.1
  # Запоминаем, что онбординг уже показывали.
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
